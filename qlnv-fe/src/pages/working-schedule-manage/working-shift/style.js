import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    container: {
        width: '100%',
        height: '100%',
        padding: '1rem'
    },
    buttonTopAction: {
        display: 'flex'
    },
    formContainer: {
        marginTop: '1rem'
    }
})